apiVersion: apps/v1newImageTag
kind: DeploymentnewImageTag
metadata:newImageTag
  name: mydeploynewImageTag
spec:newImageTag
  replicas: 1newImageTag
  selector:newImageTag
    matchLabels:newImageTag
      name: mydeploy1newImageTag
  template:newImageTag
    metadata:newImageTag
      name: mypod1newImageTag
      labels:newImageTag
        name: mydeploy1newImageTag
    spec:newImageTag
      serviceAccountName: jenkins  # Use the Jenkins ServiceAccountnewImageTag
      containers:newImageTag
      - name: mycontainernewImageTag
        image: sample:latest  # replace with your Docker-image pushed on DockerhubnewImageTag
        ports:newImageTag
        - containerPort: 8080newImageTag
 newImageTag
