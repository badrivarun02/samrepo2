spec:
  selector:
    matchLabels:
      name: mydeploy1
  replicas: 1
  template:
    spec:
      containers:
      - ports:
        - containerPort: 8080
        name: mycontainer
        image: docker push /:v
      serviceAccountName: jenkins
    metadata:
      labels:
        name: mydeploy1
      name: mypod1
metadata:
  name: mydeploy
apiVersion: apps/v1
kind: Deployment

